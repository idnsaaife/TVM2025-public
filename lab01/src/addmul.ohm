AddMul {
      Exp
    = AddExp

  AddExp = MulExp ("+" AddExp)?

  MulExp
    = PriExp ("*" MulExp)?

  PriExp
    = "(" Exp ")"  -- paren
    | number

  number  (a number)
    = digit+           
}

//digit (a digit) = "0".."9"