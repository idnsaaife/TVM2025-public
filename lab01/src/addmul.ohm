AddMul {
      Exp
    = AddExp

  AddExp = MulExp ("+" AddExp)?
    // | MulExp

  MulExp
    = PriExp "*" MulExp  -- times
    | PriExp

  PriExp
    = "(" Exp ")"  -- paren
    | number

  number  (a number)
    = digit+           
}

//digit (a digit) = "0".."9"